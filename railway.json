{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "python3 scripts/web_app.py --host 0.0.0.0 --port $PORT --bootstrap-sync",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
